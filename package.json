{
  "name": "blockchainvibe",
  "version": "1.0.0",
  "description": "BlockchainVibe - AI-powered blockchain news aggregator with Fetch.ai uAgents and SingularityNET MeTTa",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && py simple_main.py",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build:cloudflare": "cd client && npm run build && cd ../server && wrangler deploy",
    "install-all": "npm install && cd client && npm install && cd ../server && pip install -r requirements.txt",
    "deploy:worker": "cd server && wrangler deploy",
    "deploy:pages": "cd client && npm run build"
  },
  "keywords": ["ai", "news", "blockchain", "oauth", "cloudflare", "react", "fastapi"],
  "author": "AI News Agent",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
