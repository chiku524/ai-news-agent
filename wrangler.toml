# Cloudflare Pages Configuration for BlockchainVibe
# Optimized for latest Cloudflare Pages features (2024-12-10)
name = "blockchainvibe"
compatibility_date = "2024-12-10"
pages_build_output_dir = "./build"

# Production branch
production_branch = "main"

# Build configuration
[build]
command = "npm run build:ci"
cwd = "."

# Deployment configuration
[deployment]
# Automatic deployments enabled via GitHub Actions or direct wrangler deploy

# Environment variables (set via dashboard or wrangler pages secret)
# Use: wrangler pages secret put <name> [--env production|preview]
[env.production]
# Production environment variables

[env.preview]
# Preview environment variables for pull requests
